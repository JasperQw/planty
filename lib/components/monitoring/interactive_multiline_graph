import 'package:flutter/material.dart';

class InteractiveMultiLineGraph extends StatefulWidget {
  const InteractiveMultiLineGraph({Key? key}) : super(key: key);

  @override
  State<InteractiveMultiLineGraph> createState() => _InteractiveMultiLineGraphState();
}

class _InteractiveMultiLineGraphState extends State<InteractiveMultiLineGraph> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'This is an empty screen.',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}